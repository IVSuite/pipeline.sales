# Supabase project settings — find these in Supabase Dashboard > Project Settings > API
# MERGED: this app now shares the IV Operation project. Its data lives in the
# `crm` schema (the Supabase clients set db.schema = "crm"). `crm` must be added
# to the project's API "Exposed schemas".
NEXT_PUBLIC_SUPABASE_URL=https://wzochggvuavrarybyqsd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_OItPE4FEZjDOFgfIh8AEEg_RSQIxEEp

# Service role key — SERVER ONLY, never expose to the browser or commit it.
# Used sparingly for admin-only operations that must bypass Row Level Security.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
