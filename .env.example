# Supabase project settings — find these in Supabase Dashboard > Project Settings > API
# MERGED: this app now uses the IV-Suite MASTER project. Its data lives in the
# `pipeline` schema (the Supabase clients set db.schema = "pipeline"). `pipeline`
# must be added to the project's API "Exposed schemas".
NEXT_PUBLIC_SUPABASE_URL=https://hpgfwtezgrzbzqsdotkt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_GfZfm0U1mvqhSv0CLkg2yQ_G75CcoRZ

# Service role key — SERVER ONLY, never expose to the browser or commit it.
# Used sparingly for admin-only operations that must bypass Row Level Security.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
